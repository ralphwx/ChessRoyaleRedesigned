
/**
 * GameModel, but interacts with a local ServerGame object instead of through
 * the server.
 */
class LocalModel {
  constructor(user, servergame) {
    this.user = user;
    //TODO: finish this after adding getter functions to GameModel
  }
}
